quarkus:
  http:
    port: 4000
  log:
    level: INFO
  hibernate-orm:
    database:
      generation: none

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: musicfreak