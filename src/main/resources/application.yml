quarkus:
  http:
    port: 9000
  log:
    level: DEBUG
  datasource:
    db-kind: postgresql
    reactive: false
    jdbc:
      url: jdbc:postgresql://localhost:5432/musicfreak
    username: admin
    password: admin
  flyway:
    enabled: true
    migrate-at-start: true
    baseline-on-migrate: true